version: "3.8"

services:
  api:
    container_name: api
    restart: always
    image: node:24.11.1
    ports:
      - 4001:3007
    volumes:
      - ./:/usr/src/eventify
    working_dir: /usr/src/eventify

    networks:
      - monorepo-network

    # FOR PRODUCTION
    command: bash -c "rm -rf dist && rm -rf node_modules && pnpm install && pnpm run build && pnpm run start:prod"
  batch:
    container_name: batch
    restart: unless-stopped
    image: node:24.11.1
    ports:
      - 4002:3008
    volumes:
      - ./:/usr/src/eventify
    working_dir: /usr/src/eventify

    networks:
      - monorepo-network

    # FOR PRODUCTION
    command: bash -c "pnpm install && pnpm run build && pnpm run start:prod:batch"

networks:
  monorepo-network:
    driver: bridge
